spring:
  application:
    name: CartGalaxy

  datasource:
    url: jdbc:mysql://localhost:3306/cartgalaxy
    username: root
    password: root

  sql:
    init:
      mode: always

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${USER_MAIL}
    password: ${USER_PASS}

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

debug: true